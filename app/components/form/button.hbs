<button
    type="button"
    class="waves-effect waves-light btn"
    {{on "click" @onClick}}>
  
  {{#if @icon}}
    <i class="material-icons right">{{@icon}}</i>
  {{/if}}

  {{#if (has-block)}}
    {{yield}}
  {{else}}
    {{@label}}
  {{/if}}
</button>